engine-strict=true
# openapi-typescript@7.x declares peer typescript@^5.x; our project uses TS 6.
# The generator emits standard TS interfaces — compatible in practice.
# Remove this line when the maintainer widens the peer range.
legacy-peer-deps=true
