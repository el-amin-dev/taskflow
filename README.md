# TaskFlow

Team task manager with role-based access control, deadlines, and comments.

## Stack
- FastAPI (Python) + PostgreSQL + Redis
- SvelteKit (web) + Python CLI (typer)
- JWT auth, Docker Compose

## Status
🚧 In development — see build sequence in `docs/project.md`

## Setup
TBD
